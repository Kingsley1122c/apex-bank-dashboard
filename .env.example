# Core app settings
NODE_ENV=production
PORT=8787
VITE_API_BASE_URL=

# Email sender identity
EMAIL_FROM=Apex Bank <no-reply@example.com>

# SMTP configuration (optional, use this OR SendGrid)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user@example.com
SMTP_PASS=your-smtp-password

# SendGrid configuration (optional, use this OR SMTP)
SENDGRID_API_KEY=